=partial 'split-view', locals: {image: image ||= false, className: direction ||= nil } do
	%blockquote.xs-11.right
		- if block_given?
			=yield
		- else
			%p=quote
			%cite
				%p=source
				-if defined? button
					.center-text
						%a.btn.btn-readmore{:href=> button } read case study